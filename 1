# 1) 在本地创建项目文件夹
mkdir my-project && cd my-project

# 2) 初始化 git 仓库
git init

# 3) 添加至少一个文件（README）
echo "# my-project" > README.md

# 4) 提交首个版本
git add .
git commit -m "Initial commit"

# 5) 在 GitHub 网站上创建一个同名空仓库（不要勾选导入旧代码）
#    仓库创建好后，复制它的 HTTPS 或 SSH 地址，下面二选一：
#    HTTPS: https://github.com/<your-user>/my-project.git
#    SSH:   git@github.com:<your-user>/my-project.git

# 6) 关联远程并设置主分支为 main
git branch -M main
git remote add origin https://github.com/<your-user>/my-project.git   # 或 SSH 地址

# 7) 推送到 GitHub
git push -u origin main
